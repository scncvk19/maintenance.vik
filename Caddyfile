{
  admin off
}

{$APP_HOSTNAME:maintenance.local} {
  tls internal
  encode zstd gzip
  reverse_proxy frontend:3000
  header {
    X-Content-Type-Options "nosniff"
    Referrer-Policy "no-referrer"
    X-Frame-Options "SAMEORIGIN"
    -Server
  }
}
